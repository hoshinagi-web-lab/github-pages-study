# GitHub Pages 学習プロジェクト 作業ログ

## このログの目的

このファイルは、GitHub Pages で静的なHTMLサイトを公開し、後からディレクトリ単位のサイト内検索を追加するまでの作業を記録するものです。

完成したファイルだけでなく、各段階で次の内容を残します。

- 何をしようとしたか
- どのような選択をしたか
- その選択をした理由
- 実際に行った操作
- 確認した結果と、発生した問題
- 次に行うこと

このプロジェクトは個人的な学習用です。勤務先の情報、実在する業務システムの情報、第三者の著作物は使用しません。

## プロジェクトの目的

1. 複数のディレクトリと複数のHTMLファイルからなる静的サイトを作る。
2. ローカル環境でページ間のリンクと画像表示を確認する。
3. 検索機能を付ける前に、GitHub Pagesでサイトを公開する。
4. 公開したサイトをブラウザから確認する。
5. Pagefindを使ってサイト内検索を追加する。
6. サイト全体に加え、指定したディレクトリに対応するカテゴリだけを検索できるようにする。

## 段階

- [x] 第1段階：題材とサイト構成を検討する
- [x] 第2段階：HTML、CSS、画像を作成する
- [x] 第3段階：ローカル環境で表示とリンクを確認する
- [ ] 第4段階：GitHubとGitHub Pagesを設定する
- [ ] 第5段階：検索なしのサイトを公開して確認する
- [ ] 第6段階：Pagefindによる検索機能を追加する
- [ ] 第7段階：カテゴリ指定検索を含めて最終確認する

---

## 2026-09-19：プロジェクト開始

### 今回の目的

一連の作業を始める前に、題材と基本的なサイト構成を検討する。

### 決めたこと

- 学習用サイトの題材は「架空の島・星凪島の自然観察ガイド」を第一案とする。
- サイトはトップページと、地域に対応する複数のディレクトリで構成する。
- 地域の候補は「北海岸」「中央の森」「南港」とする。
- 各地域には、その地域の案内ページと複数の記事を配置する。
- 公開対象のファイルは、リポジトリ内の `docs` ディレクトリにまとめる。
- ページ間のリンクには相対パスを使い、ローカル環境とGitHub Pagesの両方で利用しやすくする。
- 文章、地名、施設、動植物などは架空の内容として作成する。
- 画像は自作の地図やイラストを使用し、外部の写真やロゴを無断使用しない。
- 最初は検索機能を付けずに公開し、正常に表示できた後でPagefindを追加する。
- 検索では「サイト全体」「北海岸」「中央の森」「南港」を選択できる構成を目指す。

### この構成を選んだ理由

地域ごとの構成はディレクトリ分けが分かりやすく、ディレクトリ単位の検索を試す題材にも適している。すべてを架空の内容にすることで、勤務先の情報や第三者の著作権に触れる危険を避けられる。また、将来アプリのバージョン別マニュアルを作る場合も、地域をバージョンに置き換えて同じ考え方を応用できる。

### 想定している公開サイトの構成

```text
docs/
├─ index.html
├─ assets/
│  ├─ css/
│  │  └─ style.css
│  └─ images/
│     ├─ island-map.svg
│     ├─ north-shore.svg
│     ├─ central-forest.svg
│     └─ south-port.svg
├─ north-shore/
│  ├─ index.html
│  ├─ access.html
│  ├─ wildlife.html
│  └─ safety.html
├─ central-forest/
│  ├─ index.html
│  ├─ trails.html
│  ├─ plants.html
│  └─ manners.html
└─ south-port/
   ├─ index.html
   ├─ facilities.html
   ├─ food.html
   └─ events.html
```

### 技術面の方針

- GitHub Pagesは、最終的に `main` ブランチの `/docs` を公開元にする予定。
- HTML、CSS、JavaScriptを中心とする静的サイトとして作成する。
- 日本語ページには `<html lang="ja">` を設定する。
- Pagefindの検索用データは、検索機能を追加する段階で静的HTMLから生成する。
- カテゴリ指定検索では、各ページに地域を表すPagefindフィルターを設定する予定。

### 現在の状態

- 作業フォルダーには、このログを作成する前のファイルは存在しなかった。
- 作業フォルダーは、まだGitリポジトリとして初期化されていない。
- HTML、CSS、画像の作成とGitHub Pagesの設定はまだ行っていない。

### 次に行うこと

第2段階として、サイトの共通デザインを決め、トップページ、地域ページ、個別記事、画像を作成する。作成後は、ページごとの役割とリンク構造をこのログに追記する。

---

## 今後の記録方法

新しい作業を行うたびに、日付ごとの項目をこのファイルの末尾に追加します。エラーが発生した場合は、エラーの概要、原因、試した対処、解決結果も記録します。GitHub上で行う設定など、ファイルに直接表れない操作も記録の対象にします。

---

## 2026-09-19：第2段階のファイル作成

### 今回の目的

第1段階で決めた構成に沿って、検索機能を含まない静的サイト一式を作成する。

### 作成したもの

- トップページ：1ページ
- 地域の入口ページ：北海岸、中央の森、南港の3ページ
- 地域別の記事：各地域3ページ、合計9ページ
- 全ページ共通のスタイルシート：1ファイル
- オリジナルのSVG画像：島全体の地図と地域別の風景、合計4点

HTMLは合計13ページとなった。トップページから各地域へ移動でき、地域の入口ページから3本の記事へ移動できる。個別記事には、トップページ、地域の入口ページ、ほかの地域へ移動するリンクを配置した。

### 記事の構成

- 北海岸：風見岬への道、潮だまりの生き物、海辺を歩く注意
- 中央の森：木漏れ日の小径、森で出会う植物、森を守る歩き方
- 南港：港の施設案内、港町の軽食、灯り舟の夕べ

### デザイン上の方針

- 海と森を連想する青緑色を基本色とした。
- パソコンと幅の狭い画面の両方で読みやすいよう、画面幅に応じて段組みを変更するCSSを設定した。
- キーボード利用者が本文へ移動できるスキップリンクを各ページに配置した。
- 現在位置が分かるよう、地域ページと記事ページにパンくずリストを配置した。
- SVG画像には、画像の内容を説明するタイトル、説明、代替テキストを設定した。
- 画像、文章、地名、施設、動植物、食品、行事はすべて今回の学習用に作成した。

### 検査結果

全HTMLファイルから `href` と `src` の参照先を抽出し、ローカルファイルが存在するか検査した。

- HTML：13ファイル
- CSS：1ファイル
- SVG：4ファイル
- 存在しないリンクまたは画像参照：0件
- `<html lang="ja">` がないページ：0件

この検査ではファイルの存在と参照関係を確認した。実際のブラウザ上の見た目、画面幅による変化、ページ間移動は第3段階で確認する。

### 次に行うこと

ローカルWebサーバーで `docs` ディレクトリを表示し、トップページ、各地域ページ、個別記事をブラウザで確認する。画像表示、ナビゲーション、文字化け、パソコン幅と狭い画面でのレイアウトを確認し、問題があれば修正する。

---

## 2026-09-19：第3段階のローカル表示確認

### 今回の目的

`docs` ディレクトリをローカルWebサーバーで配信し、実際のブラウザでページ表示、リンク、画像、画面幅によるレイアウト変化を確認する。

### 実施した操作

1. ローカルWebサーバーを起動し、`docs` を `http://localhost:8765/` で表示した。
2. トップページを開き、見出し、文章、島の地図、3地域のカード、フッターを目視確認した。
3. トップページの「北海岸の案内を見る」をクリックし、北海岸ページへ移動できることを確認した。
4. 北海岸ページの「道順を読む」をクリックし、個別記事「風見岬への道」へ移動できることを確認した。
5. 全13ページをブラウザで順番に読み込み、ページタイトル、見出し、画像、言語指定、横方向のはみ出しを確認した。
6. 画面をスマートフォン相当の幅390ピクセルに変更し、トップページと個別記事を確認した。
7. ブラウザが記録した警告とエラーを確認した。
8. テスト終了後に画面幅の指定を解除し、ローカルWebサーバーを停止した。

### 確認結果

- 全13ページが読み込まれた。
- 全ページに1つずつ適切な大見出しが表示された。
- すべてのSVG画像が正常に読み込まれた。
- デスクトップ幅で横方向の不要なスクロールは発生しなかった。
- スマートフォン幅では、ヘッダー、メイン画像、カード、記事本文、補足欄が1列に切り替わった。
- スマートフォン幅でも横方向の不要なスクロールは発生しなかった。
- 日本語の文字化けは見られなかった。
- ブラウザの警告とスクリプトエラーは0件だった。

### 見つかった問題と修正

最初の表示時に、ブラウザが自動的に要求する `favicon.ico` が存在せず、サーバーに404が1件記録された。ページの表示には影響しないが、不要なエラーを残さないため、既存の `island-map.svg` をサイトアイコンとして使う `<link rel="icon">` を全13ページに追加した。修正後、トップページでサイトアイコンの指定と参照先を確認した。

### ユーザーが行う操作

この段階では必須の操作はない。ローカルサーバーは確認終了後に停止済みである。

### 次に行うこと

第4段階としてGitリポジトリを準備し、GitHub上のリポジトリとGitHub Pagesの公開設定を行う。GitHubアカウントと公開先リポジトリに関係する操作は、現在のローカル環境とユーザーの希望を確認しながら進める。

---

## 2026-09-19：第4段階の開始とローカルGit準備

### 今回の目的

GitHubアカウントを作成する前に、ローカルで安全に進められる準備を行い、ユーザーが行うアカウント作成手順を明確にする。

### 確認結果

- Git for Windowsがインストール済みだった。
- 作業フォルダーはまだGitリポジトリではなかった。
- Gitの全体設定にはコミット作成者名と個人用メールアドレスが設定されていた。

個人用メールアドレスが公開リポジトリのコミット履歴に記録されることを避けるため、この時点では最初のコミットを作成していない。GitHubアカウント作成後、GitHubが提供する非公開用メールアドレスをこのリポジトリだけに設定してからコミットする予定である。

### 実施した操作

- `main` を最初のブランチ名として、ローカルGitリポジトリを初期化した。
- リポジトリの目的と構成を説明する `README.md` を作成した。
- OSやエディターの管理ファイルを登録しないため、`.gitignore` を作成した。
- GitHub Pagesでファイルをそのまま配信しやすくするため、`docs/.nojekyll` を追加した。
- Codexの隔離環境とフォルダー所有者が異なるためGitの所有者確認が働いたが、ユーザー全体の設定は変更せず、このリポジトリを確認するコマンドだけに安全な対象を指定した。

### 現在の状態

- ローカルリポジトリ：作成済み
- 現在のブランチ：`main`
- 最初のコミット：未作成
- GitHubアカウント：未作成
- GitHub上のリポジトリ：未作成
- GitHub Pages：未設定

### 次にユーザーが行うこと

1. GitHubの個人アカウントをGitHub Freeで作成する。
2. メールアドレスの確認を完了する。
3. 作成したGitHubユーザー名を控える。このユーザー名は公開サイトのURLに含まれる。
4. 可能であれば2要素認証を設定する。

アカウント作成後、リポジトリ専用の非公開メール設定、最初のコミット、GitHub上の公開リポジトリ作成、ファイル送信、GitHub Pages設定へ進む。

### アカウント作成後の更新

- GitHubユーザー名：`hoshinagi-web-lab`
- GitHubアカウント：作成済み
- このローカルリポジトリのコミット作成者名：`hoshinagi-web-lab` に設定済み
- GitHub CLI：このコンピューターには未導入
- GitHubのメールアドレス非公開設定：設定済み
- リポジトリ専用のコミットメール：GitHub提供の `noreply` アドレスを設定済み
- 最初のコミット：`Create initial GitHub Pages study site` として作成済み

GitHub上の公開リポジトリ名は `github-pages-study` とし、予定する公開URLは `https://hoshinagi-web-lab.github.io/github-pages-study/` となる。

個人用のGit設定は変更せず、このリポジトリのローカル設定だけを変更した。これにより、今後このリポジトリで作るコミットには個人用メールアドレスではなく、GitHub提供の非公開用アドレスが記録される。
